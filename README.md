# 🧾 OCR Pipeline – Text Extraction + PII Detection

A lightweight and efficient **OCR Pipeline** built using **Python, OpenCV, and Tesseract**.  
It extracts text from scanned documents/images and identifies sensitive information (PII) such as names, dates, IDs, age/sex, and more.

---

## 🚀 Features

- ✔️ Clean OCR extraction with preprocessing
- ✔️ Detects PII using smart regex patterns
- ✔️ Works on medical forms, invoices, scanned papers
- ✔️ Modular, readable, production-ready code
- ✔️ Easy to extend with custom PII rules

---

## 📌 How It Works

Image → Preprocessing → OCR → Regex-Based PII Extraction → JSON Output
